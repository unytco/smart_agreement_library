{
  "type": "object",
  "properties": {
    "consumed_inputs": {
      "type": "object",
      "properties": {
        "proof_of_deposit_hash": { "type": "array", "items": { "type": "string" } },
        "spender_allocations": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "amount": { "type": "object", "additionalProperties": { "type": "string" } },
              "source": { "type": "string" }
            },
            "required": ["amount", "source"]
          }
        }
      },
      "required": ["proof_of_deposit_hash", "spender_allocations"]
    },
    "inputs": {
      "type": "object",
      "properties": {
        "service_fee_wallet_address": { "type": "string" }
      },
      "required": ["service_fee_wallet_address"]
    }
  },
  "required": ["consumed_inputs", "inputs"]
}
